<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>로또 시뮬레이터 - 구매 정보 입력</title>
    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        rel="stylesheet"
    >
    <style>
        body {
            background: #f5f7fb;
        }
        .page-header {
            margin-top: 32px;
            margin-bottom: 24px;
        }
        .lotto-badge {
            font-weight: 700;
            letter-spacing: 1px;
        }
        .lotto-card {
            border-radius: 12px;
        }
        .ticket-list {
            list-style: none;
            padding-left: 0;
            margin-bottom: 0;
        }
        .ticket-list li {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-bottom: 4px;
        }
        .lotto-ball {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
            font-weight: 600;
            color: #fff;
        }
        .lotto-range-1 { background: #f94144; }
        .lotto-range-2 { background: #f8961e; }
        .lotto-range-3 { background: #f9c74f; color:#333; }
        .lotto-range-4 { background: #43aa8b; }
        .lotto-range-5 { background: #577590; }
        .section-title {
            font-weight: 600;
            margin-bottom: 8px;
        }
        .subtle-card {
            background: #ffffff;
            border-radius: 12px;
        }
        .mode-chip {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 4px 10px;
            border-radius: 999px;
            background: #e9f2ff;
            font-size: 13px;
        }
        .mode-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: #0d6efd;
        }
    </style>
</head>
<body>
<div class="container my-4">
    <header class="page-header">
        <div class="d-flex justify-content-between align-items-center flex-wrap gap-2">
            <div>
                <span class="badge bg-primary lotto-badge">LOTTO SIMULATOR</span>
                <h2 class="mt-2 mb-0 fw-bold">플레이어 정보 입력</h2>
                <p class="text-muted mb-0">
                    각 플레이어의 이름과 구매 금액을 입력하면, 로또 번호가 자동으로 발행됩니다.
                </p>
            </div>
            <div class="mode-chip">
                <span class="mode-dot"></span>
                <span>
                    모드:
                    {{if eq .Mode 0}}고정 상금 모드{{else}}분배(패리뮤추얼) 모드{{end}}
                </span>
            </div>
        </div>
    </header>

    {{with .Error}}
    <div class="alert alert-danger mb-4">
        {{.}}
    </div>
    {{end}}

    <div class="card lotto-card shadow-sm border-0 mb-4">
        <div class="card-body p-4">
            <h5 class="section-title">플레이어 정보</h5>
            <p class="text-muted">
                플레이어 수: <strong>{{.Count}}</strong> 명
            </p>

            <form action="/purchase" method="post" class="vstack gap-3">
                <input type="hidden" name="mode" value="{{.Mode}}">
                <input type="hidden" name="count" value="{{.Count}}">

                {{range $idx, $n := .IndexList}}
                    <div class="row g-3 border-bottom pb-3 mb-3">
                        <div class="col-md-5">
                            <label class="form-label fw-semibold">
                                플레이어 {{$n}} 이름
                            </label>
                            <input type="text"
                                   name="name{{$n}}"
                                   class="form-control"
                                   placeholder="예: 플레이어{{$n}}">
                        </div>
                        <div class="col-md-5">
                            <label class="form-label fw-semibold">
                                구매 금액 (원)
                            </label>
                            <input type="number"
                                   name="amount{{$n}}"
                                   class="form-control"
                                   min="1000" step="1000"
                                   placeholder="예: 8000">
                            <div class="form-text">
                                1장당 {{money $.LottoPrice}}원 단위로 입력해 주세요.
                            </div>
                        </div>
                    </div>
                {{end}}

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-primary">
                        로또 발행하기
                    </button>
                </div>
            </form>
        </div>
    </div>

    {{if .Players}}
    <div class="row">
        <div class="col-lg-7 mb-4">
            <div class="card subtle-card shadow-sm">
                <div class="card-body p-4">
                    <h5 class="section-title">발행된 로또 티켓</h5>
                    <p class="text-muted mb-3">
                        각 플레이어에게 발행된 로또 번호입니다. 번호는 구간별 색을 적용했습니다.
                    </p>

                    {{range $pi, $p := .Players}}
                        <div class="mb-3">
                            <h6 class="fw-semibold mb-1">
                                {{$p.Name}}
                                <small class="text-muted">
                                    (구매 금액: {{money $p.Amount}}원)
                                </small>
                            </h6>
                            <ul class="ticket-list">
                                {{range $ti, $t := $p.Tickets}}
                                    <li>
                                        {{range $t.Numbers}}
                                            <span class="lotto-ball" data-num="{{.}}">{{.}}</span>
                                        {{end}}
                                    </li>
                                {{end}}
                            </ul>
                        </div>
                    {{end}}

                    <div class="mt-3 text-muted">
                        총 구매 금액: <strong>{{money .TotalSales}}</strong>원
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-5 mb-4">
            <div class="card subtle-card shadow-sm">
                <div class="card-body p-4">
                    <h5 class="section-title">당첨 번호 입력</h5>
                    <p class="text-muted mb-3">
                        발행된 티켓을 기준으로 당첨 번호와 보너스 번호를 입력하면, 최종 통계를 계산합니다.
                    </p>

                    <form action="/result" method="post" class="vstack gap-3">
                        <input type="hidden" name="mode" value="{{.Mode}}">
                        <input type="hidden" name="count" value="{{.Count}}">
                        <input type="hidden" name="totalSales" value="{{.TotalSales}}">

                        {{range $i, $p := .Players}}
                            {{$idx := add1 $i}}
                            <input type="hidden" name="name{{$idx}}" value="{{$p.Name}}">
                            <input type="hidden" name="amount{{$idx}}" value="{{$p.Amount}}">
                            {{range $tIdx, $t := $p.Tickets}}
                                <input type="hidden"
                                       name="ticket_{{$idx}}_{{$tIdx}}"
                                       value="{{joinInts $t.Numbers ","}}">
                            {{end}}
                        {{end}}

                        <div>
                            <label class="form-label fw-semibold">
                                당첨 번호 (쉼표로 구분, 예: 1,2,3,4,5,6)
                            </label>
                            <input type="text"
                                   name="winningNumbers"
                                   class="form-control"
                                   placeholder="1,2,3,4,5,6"
                                   value="{{.WinningInput}}">
                        </div>

                        <div>
                            <label class="form-label fw-semibold">
                                보너스 번호
                            </label>
                            <input type="number"
                                   name="bonusNumber"
                                   class="form-control"
                                   min="1" max="45"
                                   placeholder="예: 7"
                                   value="{{.BonusInput}}">
                            <div class="form-text">
                                당첨 번호 6개와 중복되지 않도록 입력해 주세요.
                            </div>
                        </div>

                        <div class="d-flex justify-content-end">
                            <button type="submit" class="btn btn-success">
                                최종 결과 보기
                            </button>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
    {{end}}
</div>

<script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js">
</script>
<script>
    // 구간에 따라 다른 색상 적용
    document.addEventListener("DOMContentLoaded", function () {
        document.querySelectorAll(".lotto-ball").forEach(function (el) {
            var n = parseInt(el.dataset.num, 10);
            var cls = "lotto-range-1";
            if (n >= 1 && n <= 10) cls = "lotto-range-1";
            else if (n >= 11 && n <= 20) cls = "lotto-range-2";
            else if (n >= 21 && n <= 30) cls = "lotto-range-3";
            else if (n >= 31 && n <= 40) cls = "lotto-range-4";
            else if (n >= 41 && n <= 45) cls = "lotto-range-5";
            el.classList.add(cls);
        });
    });
</script>
</body>
</html>
